# 生成对抗网络（GAN）实验报告

## 1. 实验目的

生成对抗网络（GAN）是一种强大的深度学习模型，能够学习生成与真实数据分布相似的新数据样本。本实验的主要目的包括：

1. **深入理解 GAN 的基本原理**：通过实现一个基础的 GAN 模型，掌握生成器（Generator）和判别器（Discriminator）的对抗训练机制
2. **掌握 GAN 的训练流程**：理解 GAN 的损失函数设计，包括判别器的二元交叉熵损失和生成器的对抗损失
3. **探索不同超参数对训练的影响**：通过对比不同优化器（Adam、SGD）和学习率的实验，分析其对生成结果的影响
4. **学习评估生成模型的方法**：通过观察损失曲线、D(x) 和 D(G(z)) 的变化趋势，评估 GAN 的训练效果

## 2. 实验要求

本实验要求在 Fashion-MNIST 数据集上实现并训练一个 GAN 模型，具体要求如下：

1. **数据集**：使用 Fashion-MNIST 数据集，包含 60,000 张训练图像和 10,000 张测试图像，每张图像为 28×28 灰度图
2. **网络架构**：
   - 生成器：使用全连接网络，输入为 100 维噪声向量，输出为 28×28 图像
   - 判别器：使用全连接网络，输入为 28×28 图像，输出为真实概率
3. **训练策略**：
   - 使用二元交叉熵损失函数（BCELoss）
   - 对比不同优化器（Adam vs SGD）和学习率的效果
   - 训练至少 10 个 epoch
4. **评估指标**：
   - 监控判别器损失（Loss_D）和生成器损失（Loss_G）
   - 观察 D(x)（判别器对真实图像的判断）和 D(G(z))（判别器对生成图像的判断）的变化
5. **结果展示**：
   - 生成不同训练阶段的图像样本
   - 绘制损失曲线对比图

## 3. 实验代码

### 3.1 网络架构定义

```python
import torch
import torch.nn as nn

class Discriminator(torch.nn.Module):
    def __init__(self, inp_dim=784):
        super(Discriminator, self).__init__()
        self.fc1 = nn.Linear(inp_dim, 128)
        self.nonlin1 = nn.LeakyReLU(0.2)
        self.fc2 = nn.Linear(128, 1)
    def forward(self, x):
        x = x.view(x.size(0), 784)  # flatten (bs x 1 x 28 x 28) -> (bs x 784)
        h = self.nonlin1(self.fc1(x))
        out = self.fc2(h)
        out = torch.sigmoid(out)
        return out

class Generator(nn.Module):
    def __init__(self, z_dim=100):
        super(Generator, self).__init__()
        self.fc1 = nn.Linear(z_dim, 128)
        self.nonlin1 = nn.LeakyReLU(0.2)
        self.fc2 = nn.Linear(128, 784)
    def forward(self, x):
        h = self.nonlin1(self.fc1(x))
        out = self.fc2(h)
        out = torch.tanh(out)  # range [-1, 1]
        out = out.view(out.size(0), 1, 28, 28)
        return out
```

### 3.2 训练函数

```python
def train_gan(epochs=10, lr_d=0.0002, lr_g=0.0002, optimizer_type='Adam', experiment_name='exp1'):
    """训练GAN并记录损失和生成图像"""
    device = torch.device('cuda') if torch.cuda.is_available() else torch.device('cpu')

    # 初始化网络
    D = Discriminator().to(device)
    G = Generator().to(device)

    # 设置优化器
    if optimizer_type == 'Adam':
        optimizerD = torch.optim.Adam(D.parameters(), lr=lr_d, betas=(0.5, 0.999))
        optimizerG = torch.optim.Adam(G.parameters(), lr=lr_g, betas=(0.5, 0.999))
    else:  # SGD
        optimizerD = torch.optim.SGD(D.parameters(), lr=lr_d)
        optimizerG = torch.optim.SGD(G.parameters(), lr=lr_g)

    criterion = nn.BCELoss()
    fixed_noise = torch.randn(64, 100, device=device)

    # 记录损失
    G_losses = []
    D_losses = []
    img_list = []

    for epoch in range(epochs):
        for i, data in enumerate(dataloader, 0):
            # 更新判别器 D
            D.zero_grad()
            real_cpu = data[0].to(device)
            b_size = real_cpu.size(0)
            label = torch.full((b_size,), 1., dtype=torch.float, device=device)

            output = D(real_cpu).view(-1)
            errD_real = criterion(output, label)
            errD_real.backward()
            D_x = output.mean().item()

            noise = torch.randn(b_size, 100, device=device)
            fake = G(noise)
            label.fill_(0.)
            output = D(fake.detach()).view(-1)
            errD_fake = criterion(output, label)
            errD_fake.backward()
            D_G_z1 = output.mean().item()
            errD = errD_real + errD_fake
            optimizerD.step()

            # 更新生成器 G
            G.zero_grad()
            label.fill_(1.)
            output = D(fake).view(-1)
            errG = criterion(output, label)
            errG.backward()
            D_G_z2 = output.mean().item()
            optimizerG.step()

            if i % 50 == 0:
                G_losses.append(errG.item())
                D_losses.append(errD.item())

        # 每个epoch结束后保存生成图像
        with torch.no_grad():
            fake = G(fixed_noise).detach().cpu()
        img_list.append(vutils.make_grid(fake, nrow=8, normalize=True))

    return G_losses, D_losses, img_list, G, D
```

### 3.3 主实验代码

```python
import torchvision.datasets
import torchvision.transforms as transforms
import torchvision.utils as vutils
import matplotlib.pyplot as plt
import numpy as np

# 加载Fashion MNIST数据集
dataset = torchvision.datasets.FashionMNIST(root='./FashionMNIST/',
                       transform=transforms.Compose([transforms.ToTensor(),
                                                     transforms.Normalize((0.5,), (0.5,))]),
                       download=True)
dataloader = torch.utils.data.DataLoader(dataset, batch_size=64, shuffle=True)

# 运行实验1：Adam优化器，lr=0.0002
G_losses1, D_losses1, img_list1, G1, D1 = train_gan(
    epochs=10, lr_d=0.0002, lr_g=0.0002, optimizer_type='Adam', experiment_name='Adam_lr0.0002'
)

# 运行实验2：SGD优化器，lr=0.03
G_losses2, D_losses2, img_list2, G2, D2 = train_gan(
    epochs=10, lr_d=0.03, lr_g=0.03, optimizer_type='SGD', experiment_name='SGD_lr0.03'
)

# 运行实验3：Adam优化器，lr=0.001
G_losses3, D_losses3, img_list3, G3, D3 = train_gan(
    epochs=10, lr_d=0.001, lr_g=0.001, optimizer_type='Adam', experiment_name='Adam_lr0.001'
)
```

## 4. 实验结果

### 4.1 损失曲线对比

下图展示了三种不同超参数配置下的损失曲线对比：

![损失曲线对比](outputs/experiment_comparison.png)

**观察与分析**：

1. **Adam lr=0.0002**：
   - 判别器损失（D_loss）稳定在 0.6-0.9 之间
   - 生成器损失（G_loss）逐渐上升，表明生成器在学习如何欺骗判别器
   - D(x) 保持在 0.7-0.8，D(G(z)) 逐渐下降到 0.2 以下

2. **SGD lr=0.03**：
   - 损失波动较大，训练不稳定
   - 判别器和生成器损失交替剧烈变化
   - 出现模式崩溃（mode collapse）的迹象

3. **Adam lr=0.001**：
   - 损失曲线相对稳定
   - 判别器损失在 1.0 左右，生成器损失在 1.5 左右
   - 训练效果介于前两者之间

### 4.2 生成图像对比

下图展示了三种配置在训练结束时的生成图像：

| Adam lr=0.0002 | SGD lr=0.03 | Adam lr=0.001 |
|:---:|:---:|:---:|
| ![Adam 0.0002](outputs/adam_lr0.0002_epoch9.png) | ![SGD 0.03](outputs/sgd_lr0.03_epoch9.png) | ![Adam 0.001](outputs/adam_lr0.001_epoch9.png) |

**观察与分析**：
- **Adam lr=0.0002** 生成的图像质量最好，能够生成类似 Fashion-MNIST 数据集中的服装图像
- **SGD lr=0.03** 生成的图像质量较差，存在明显的噪声和失真
- **Adam lr=0.001** 生成的图像质量中等，但不如 Adam lr=0.0002 清晰

### 4.3 训练过程可视化

下图展示了 Adam lr=0.0002 配置下不同训练阶段的生成图像演变：

![训练过程](outputs/epoch_progression.png)

**观察与分析**：
- 第 1 个 epoch：生成的图像几乎全是噪声
- 第 3-5 个 epoch：开始出现模糊的服装轮廓
- 第 7-10 个 epoch：生成的图像逐渐清晰，能够看出服装的基本形状

### 4.4 实验结论

1. **优化器选择**：Adam 优化器在 GAN 训练中表现优于 SGD，能够提供更稳定的梯度更新
2. **学习率影响**：
   - 较小的学习率（0.0002）有利于稳定训练
   - 较大的学习率（0.03）会导致训练不稳定
3. **训练时长**：10 个 epoch 的训练能够生成基本可辨的图像，但更长的训练可能会产生更好的结果
4. **GAN 训练的挑战**：GAN 训练容易出现模式崩溃和训练不稳定的问题，需要仔细调整超参数

## 5. 参考资料

- [Generative Adversarial Networks (Goodfellow et al., 2014)](https://arxiv.org/abs/1406.2661)
- [DCGAN (Radford et al., 2015)](https://arxiv.org/abs/1511.06434)
- PyTorch 官方 GAN 教程
