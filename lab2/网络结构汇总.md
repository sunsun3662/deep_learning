# Lab2 网络结构汇总

## RNNClassifier

```text
RNNClassifier(
  (embed): Embedding(58, 64, padding_idx=0)
  (classifier): Linear(in_features=128, out_features=18, bias=True)
  (log_softmax): LogSoftmax(dim=1)
  (rnn): RNN(64, 128, batch_first=True)
)
```

## LSTMClassifier

```text
LSTMClassifier(
  (embed): Embedding(58, 64, padding_idx=0)
  (classifier): Linear(in_features=128, out_features=18, bias=True)
  (log_softmax): LogSoftmax(dim=1)
  (lstm): LSTM(64, 128, batch_first=True)
)
```
